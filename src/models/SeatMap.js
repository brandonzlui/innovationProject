class SeatMap {

  constructor(name, rows, seatsPerRow, available, aisle, window) {
    this.name = name
    this.rows = rows
    this.seatsPerRow = seatsPerRow
    this.available = available
    this.aisle = aisle
    this.window = window
  }

}

module.exports = SeatMap